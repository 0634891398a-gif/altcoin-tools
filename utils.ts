type GameState = { level: number; score: number; playerName: string; timestamp: number; };  const optimizeGameStates = (states: GameState[]): GameState[] => {   const seen: { [key: string]: GameState } = {};   return states.filter(state => {     const key = `${state.playerName}-${state.level}`;     if (seen[key]) return false;     seen[key] = state;     return true;   }); };  const computeHighScore = (states: GameState[]): number => {   return Math.max(...states.map(state => state.score)); };  export { GameState, optimizeGameStates, computeHighScore };