interface GameConfig {  maxPlayers: number;  gameName: string;  minPlayers: number;  validate: () => boolean;}  class Config implements GameConfig {  maxPlayers: number;  minPlayers: number;  gameName: string;   constructor(maxPlayers: number, minPlayers: number, gameName: string) {    this.maxPlayers = maxPlayers;    this.minPlayers = minPlayers;    this.gameName = gameName;  }   validate(): boolean {    if (this.maxPlayers <= 0 || this.minPlayers <= 0 || this.minPlayers > this.maxPlayers) {      console.error('Invalid player configuration');      return false;    }    return true;  }}  const gameConfig = new Config(10, 2, 'Fantasy Arena');  if (!gameConfig.validate()) {  throw new Error('Configuration is invalid');}  export default gameConfig;  